spring:
  application:
    name: apollo-adminservice
  profiles:
    active: ${apollo_profile}

ctrip:
  appid: 100003172
  
server:
  port: 8080
  
logging:
  file: /data2/log/apollo/100003172/apollo-adminservice.log
